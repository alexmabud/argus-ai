"""Testes e2e de frontend (Playwright) — opt-in.

Exigem `playwright` instalado e o browser Chromium baixado
(`pip install playwright && playwright install chromium`).
Sem isso, os testes são pulados sem quebrar a suíte.
"""
