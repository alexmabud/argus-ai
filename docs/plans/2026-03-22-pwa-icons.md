# PWA Icons Implementation Plan

> **For Claude:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** Gerar ícones reais para o PWA do Argus AI a partir da imagem do olho cyberpunk, substituindo os placeholders em branco.

**Architecture:** Script Python standalone com Pillow que processa a imagem original, remove o fundo branco (substituindo por #050A0F), faz crop quadrado centralizado, e salva nas duas dimensões exigidas pelo manifest.json.

**Tech Stack:** Python 3.11+, Pillow (já disponível no ambiente)

---

### Task 1: Gerar os ícones com script Python

**Files:**
- Create: `scripts/generate_icons.py`
- Modify: `frontend/icons/icon-192.png` (sobrescrito pelo script)
- Modify: `frontend/icons/icon-512.png` (sobrescrito pelo script)

**Step 1: Verificar que Pillow está instalado**

```bash
python -c "from PIL import Image; print('OK')"
```

Se falhar: `pip install Pillow`

**Step 2: Criar o script**

Criar `scripts/generate_icons.py`:

```python
"""Script para gerar ícones PWA do Argus AI a partir da imagem do olho cyberpunk.

Processa a imagem original removendo o fundo branco e substituindo
pelo fundo escuro do app (#050A0F), depois redimensiona para os
tamanhos exigidos pelo manifest.json.
"""

from pathlib import Path
from PIL import Image
import numpy as np

SOURCE = Path(r"C:\Users\User\Downloads\Phone Link nome do arquivo 1772505697458.png")
OUT_DIR = Path(__file__).parent.parent / "frontend" / "icons"
BG_COLOR = (5, 10, 15)  # #050A0F

def remove_white_bg(img: Image.Image) -> Image.Image:
    """Remove o fundo branco e substitui pelo fundo escuro do app.

    Args:
        img: Imagem original em qualquer modo.

    Returns:
        Imagem RGBA com fundo branco substituído por #050A0F.
    """
    img = img.convert("RGBA")
    data = np.array(img)

    r, g, b, a = data[:, :, 0], data[:, :, 1], data[:, :, 2], data[:, :, 3]
    mask = (r > 200) & (g > 200) & (b > 200)

    data[mask] = [*BG_COLOR, 255]
    return Image.fromarray(data, "RGBA")


def crop_square(img: Image.Image) -> Image.Image:
    """Faz crop quadrado centralizado na imagem.

    Args:
        img: Imagem de entrada.

    Returns:
        Imagem quadrada centralizada.
    """
    w, h = img.size
    size = min(w, h)
    left = (w - size) // 2
    top = (h - size) // 2
    return img.crop((left, top, left + size, top + size))


def generate(source: Path, out_dir: Path) -> None:
    """Gera icon-192.png e icon-512.png a partir da imagem fonte.

    Args:
        source: Caminho da imagem original do olho cyberpunk.
        out_dir: Diretório de saída (frontend/icons/).
    """
    out_dir.mkdir(parents=True, exist_ok=True)

    img = Image.open(source)
    img = remove_white_bg(img)
    img = crop_square(img)

    for size in (512, 192):
        resized = img.resize((size, size), Image.LANCZOS)
        # Converter para RGB antes de salvar (remove canal alpha)
        final = Image.new("RGB", (size, size), BG_COLOR)
        final.paste(resized, mask=resized.split()[3])
        out_path = out_dir / f"icon-{size}.png"
        final.save(out_path, "PNG", optimize=True)
        print(f"Gerado: {out_path} ({out_path.stat().st_size // 1024} KB)")


if __name__ == "__main__":
    generate(SOURCE, OUT_DIR)
```

**Step 3: Executar o script**

```bash
cd c:/projetos/argus_ai
python scripts/generate_icons.py
```

Saída esperada:
```
Gerado: frontend/icons/icon-512.png (XX KB)
Gerado: frontend/icons/icon-192.png (XX KB)
```

Os arquivos devem ter tamanho bem maior que os placeholders (> 10 KB cada).

**Step 4: Verificar os arquivos gerados**

```bash
ls -lh frontend/icons/
```

Confirmar que ambos os arquivos têm tamanho significativo (não 593 bytes).

**Step 5: Commit**

```bash
git add scripts/generate_icons.py frontend/icons/icon-192.png frontend/icons/icon-512.png
git commit -m "feat(pwa): adicionar ícones reais do olho cyberpunk para instalação PWA"
```

---

### Task 2: Testar a instalação no Chrome

**Step 1: Subir o servidor local**

```bash
make dev
```

**Step 2: Abrir o Chrome em `http://localhost:8000`**

**Step 3: Verificar o manifest no DevTools**

Abrir DevTools → Application → Manifest — confirmar que os ícones aparecem com preview real (não em branco).

**Step 4: Instalar o PWA**

Clicar no ícone de instalação na barra de endereços do Chrome (ou Menu → "Instalar Argus AI").
Confirmar que o ícone do olho cyberpunk aparece na tela inicial/desktop.
